#include <stdio.h>
#include <stdlib.h>
/**
 * main - Point of entry
 * Return: 0 always
 */
int main(void)
{
	char letter;

	for (letter = 'a' ; letter <= 'z'; letter++)
		putchar(letter);
	putchar('\n");
	return (0);
}
