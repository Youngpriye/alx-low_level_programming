#include <stdio.h>
/**
 * main - Entry point for integers
 * Description: Prints base 10 digits
 * Return: 0 sucess
 */

int main(void)
{
	int x;

	for (x = 0 ; x < 10 ; x++)
	{
		printf("%d", x);
	}
	putchar('\n');
	return (0);
}
